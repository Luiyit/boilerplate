<header class="header">

  {# Top menu desktop #}
  {% include "main-top-desktop.twig" with { 'items': menu.get_items } %}

  {# Top Menu Mobile #}
  {% include "main-top-mobile.twig" with { 'items': menu.get_items } %}
  
  {# Sticky #}
  <div id="sticky-container" class="enabled-admin-padding">
    <nav id="nav-main" class="nav-main" role="navigation">
      <div class="wrapper" style="display: none;">
        {# Sticky Desktop  #}
        {% include "sticky-desktop.twig" with { 'items': menu.get_items } %}
        
        {# Sticky Mobile  #}
        {% include "sticky-mobile.twig" with { 'items': menu.get_items } %}
      </div>
    </nav>
  </div>
</header>